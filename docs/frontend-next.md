# Dockru Frontend

- [ ] on the /terminal/ pages there stack title should be a link back to the stack
- [ ] on the /compose/ pages there should be a button, above the bash/terimnal button, to view logs specifically for that container
- [ ] the current 2 column layout in /compose/ should be the "classic" layout, and a user setting should be added for "dockru" layout
  - [ ] the "dockru" layout should be a tabbed interface with url-path routing for
    - [ ] containers
    - [ ] compose.yaml
    - [ ] stack logs
- [ ] the stack list search icon should be visibly connected to the search bar, and the search=icon+input should be full width in its container